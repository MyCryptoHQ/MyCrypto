export interface AddressMetadata {
  address: string;
  label: string;
  notes: string;
}

export interface ExtendedAddressMetadata {
  address: string;
  label: string;
  notes: string;
  uuid: string;
}
