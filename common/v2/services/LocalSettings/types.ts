export interface LocalSetting {
  label: string;
  address: string;
  network: string;
}

export interface ExtendedLocalSetting {
  label: string;
  address: string;
  network: string;
  uuid: string;
}
