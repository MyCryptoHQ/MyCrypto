/*import BN from "bn.js";
import { isValidHex } from "../validators";
import { stripHexPrefix } from "./formatters";


export const gweiGasPriceToWei = (gasPrice: string | number): BN => {
  
  if (typeof(gasPrice) === 'string') {
    // If gasPrice is a string
    if (isValidHex(gasPrice)) {
      // If gasPrice is a Hex string
      const gasPriceNum = parseInt(stripHexPrefix(gasPrice), 16)
      return (gasPriceNum)
    } else {
      const 
    }
  }

  // If gasPrice is a number
  if (typeof(gasPrice) === 'number') {
  
  }
}

export const gasPriceFromWei = (gasPrice: BN, type?: string, ) => {
  
}*/
