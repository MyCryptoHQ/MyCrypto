import { TransactionFormData, ConfirmTransaction, TransactionComplete } from './components';

export const steps = [TransactionFormData, ConfirmTransaction, TransactionComplete];

export const headings = ['Send Assets', 'Confirm Transaction', 'Transaction Complete'];
