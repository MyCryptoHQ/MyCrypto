import { ConfirmTransaction, SendAssetsForm, TransactionComplete } from './components';

export const steps = [SendAssetsForm, ConfirmTransaction, TransactionComplete];

export const headings = ['Send Assets', 'Confirm Transaction', 'Transaction Complete'];
